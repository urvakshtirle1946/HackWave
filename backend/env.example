# Database Configuration
# For Aiven/managed Postgres, SSL is typically required. Include `sslmode=require`.
# Example (replace placeholders):
# DATABASE_URL="postgresql://<USER>:<PASSWORD>@<HOST>:<PORT>/<DATABASE>?sslmode=require"
DATABASE_URL="postgresql://username:password@localhost:5432/supply_chain_db?sslmode=require"

# Server Configuration
# Keep this in sync with the frontend dev proxy (frontend/vite.config.ts)
PORT=3001
NODE_ENV=development

# GroqCloud API Configuration
GROQ_API_KEY="your_groq_api_key_here"

# Optional: Other API Keys
# OPENAI_API_KEY="your_openai_api_key_here"
# ANTHROPIC_API_KEY="your_anthropic_api_key_here"
